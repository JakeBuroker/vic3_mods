### Готово: конфликты TGR ↔ BPM найдены и перепроверены

  Сгенерировал и вручную перепроверил конфликты между `@.TheGreatRevision` и `@.BPM` (с учётом того, что **BPM грузится последним**).

- **Авто-отчёт сканера**: `bpm_tgr/conflicts_tgr_vs_bpm_report.md`  
  - **45** жёстких пересечений по пути файла  
  - **157** дублей ключей в `common/**` (эвристика)  
  - **6** дублей ключей локализации  
  - **0** дублей event id
- **Ручная выжимка + уточнения (что реально важно/что ложноположительное)**: `bpm_tgr/conflicts_tgr_vs_bpm_summary.md`

### Самые критичные конфликты (которые точно сломают/перезапишут логику)

- *`common/decisions/manifest_destiny.txt`**: в TGR файл с логикой, в BPM файл **пустой** → при загрузке BPM последним это фактически **убивает** и ванильное, и TGR-решение.
- **Налоги** `common/laws/*taxation*`**)**: TGR отключает реальные `tax_modifier_*` (логика уходит в tax panel переменные), BPM возвращает полноценные `tax_modifier_*` → это **структурный конфликт систем**.
- **Политический стек**: `law_groups`, `laws`, `ideologies`, `interest_groups`, `parties`, `legitimacy_levels`, `government_types`, `journal_entries` — там совпадают ключи, значит без компача будет “победитель по порядку загрузки”.

### Доп. проверка вне `common/`

- Жёсткие оверрайды вне `common` — в основном *`gfx/**` (41 файл)**, т.е. в текущем порядке “победит” внешний вид BPM.
- `gui/*.gui`: пересечений идентификаторов **нет** `bpm_tgr/gui_identifiers_tgr_vs_bpm.txt`).  
Когда скажешь — начнём собирать компач в `@bpm_tgr` (уже подготовлена папка и отчёты).

