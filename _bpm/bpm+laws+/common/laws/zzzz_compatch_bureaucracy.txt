INJECT:law_hereditary_bureaucrats = { # 세습 관료제
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/hereditary_bureaucrats.dds"

	progressiveness = -50

	is_visible = { # Laws +
		NAND = {
			exists = c:RUS
			c:RUS = ROOT
			NOT = { has_law = law_type:law_hereditary_bureaucrats }
		}
	}

	modifier = {
		state_bureaucracy_population_base_cost_factor_mult = -0.25
		country_aristocrats_pol_str_mult = 0.25
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				OR = {
					has_law = law_type:law_meritocratic_bureaucracy	# Laws +
					has_law = law_type:law_elected_bureaucrats
				}
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	disallowing_laws = {
		law_council_republic
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

INJECT:law_mestnichestvo = { # 메스트니체스트보	# Laws +
	group = lawgroup_bureaucracy
	parent = law_hereditary_bureaucrats

	icon = "gfx/interface/icons/law_icons/mestnichestvo.dds"

	progressiveness = -100

	is_visible = {
		exists = c:RUS
		c:RUS = ROOT
		NOT = { has_law = law_type:law_hereditary_bureaucrats }
	}

	can_enact = {
		NOT = { has_law = law_type:law_council_republic }
		ig:ig_landowners = {
			is_in_government = yes
			is_marginal = no
		}
	}

	modifier = {
		country_authority_mult = 0.5
		interest_group_ig_landowners_pol_str_mult = 0.5
	}

	on_enact = {
		custom_tooltip = QUALIFICATIONS_OFFICERS_PEASANT_LEVIES_DESC
		if = {
			limit = {
				OR = {
					has_law = law_type:law_meritocratic_bureaucracy	# Laws +
					has_law = law_type:law_elected_bureaucrats
				}
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

INJECT:law_appointed_bureaucrats = { # 임명 관료제
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/appointed_bureaucrats.dds"

	progressiveness = 0

	modifier = {
		country_bureaucrats_pol_str_mult = 0.25
		state_tax_capacity_mult = 0.25
	}

	on_enact = {
		if = {
			limit = {
				OR = {
					has_law = law_type:law_elected_bureaucrats
					has_law = law_type:law_meritocratic_bureaucracy	# Laws +
				}
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				OR = {
					has_journal_entry = je_sick_man_bureaucracy
					has_journal_entry = je_great_reforms_bureaucratic
				}
				has_law = law_type:law_hereditary_bureaucrats
			}
			add = 1000
		}
	}
}

INJECT:law_gwageo = { # 과거 제도	# Laws +
	group = lawgroup_bureaucracy
	parent = law_appointed_bureaucrats

	icon = "gfx/interface/icons/law_icons/imperial_examination.dds"

	progressiveness = 0

	requires_law_or = {
		law_state_religion
		law_freedom_of_conscience
	}

	is_visible = {
		country_has_state_religion = rel:confucian
	}

	can_enact = {
		country_has_state_religion = rel:confucian
		OR = {
			has_law = law_type:law_state_religion
			has_law = law_type:law_freedom_of_conscience
		}
		OR = {
			ig:ig_devout = {
				is_in_government = yes
				is_marginal = no
			}
			ig:ig_landowners = {
				is_in_government = yes
				is_marginal = no
			}
		}
	}

	modifier = {
		state_tax_capacity_mult = 0.35
		interest_group_ig_landowners_pol_str_mult = 0.25
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

INJECT:law_meritocratic_bureaucracy = { # 능력주의 관료제	# Laws +
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/meritocratic_bureaucracy.dds"

	progressiveness = 100

	modifier = {
		building_government_administration_throughput_add = 0.35
		interest_group_ig_petty_bourgeoisie_pol_str_mult = 0.25
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = { 
				OR = {
					has_journal_entry = je_sick_man_bureaucracy
					has_journal_entry = je_great_reforms_bureaucratic
				}
				has_law = law_type:law_hereditary_bureaucrats
			}
			add = 1000
		}
	}
}

INJECT:law_elected_bureaucrats = { # 선출 관료제
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/elected_bureaucrats.dds"

	progressiveness = 50

	disallowing_laws = {
		law_autocracy 
		law_oligarchy
		law_technocracy
		law_elder_council
		law_ecclesiarchy		# Laws +
		law_semi_constitutional	# Laws +
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	modifier = {
		country_bureaucracy_investment_cost_factor_mult = -0.15
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}
